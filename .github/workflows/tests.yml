name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: roamwise/requirements.txt

      - name: Install dependencies
        run: |
          pip install -r roamwise/requirements.txt
          pip install pytest

      - name: Run test suite
        run: |
          cd roamwise
          pytest tests/ -v
